import { defineConfig } from 'vite';
import { svelte } from '@sveltejs/vite-plugin-svelte';

export default defineConfig({
  plugins: [svelte()],
  server: {
    port: 9000,
    allowedHosts: ['moose', 'moose.local', 'minibini.me', 'minibini-vite'],
    proxy: {
      '/api': 'http://minibini-django:9000',
    },
  },
});

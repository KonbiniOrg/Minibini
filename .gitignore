venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
db.sqlite3
.env
*.log
pyvenv.cfg
.vscode/
.DS_Store
minibini/settings_local.py
Minibini.code-workspace
Miniterminal.code-workspace
database_commands.txt
runserver_commands.txt
.superpowers/
.worktrees/
media/
nealsdata/datasets/
# Claude Code scratch stays ignored, but the team-shared config
# (hooks + project settings) is committed so it applies on every machine.
.claude/*
!.claude/settings.json
!.claude/hooks/

# Local temp DB dumps (may contain tokens/sessions)
tempfixture.json
